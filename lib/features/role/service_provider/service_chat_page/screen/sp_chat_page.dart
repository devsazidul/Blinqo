import 'package:flutter/material.dart';

class SpChatPage extends StatelessWidget {
  const SpChatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: const Text('Chat Page Content')));
  }
}
