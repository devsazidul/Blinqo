import 'package:flutter/material.dart';

class VenueChatScreen extends StatelessWidget {
  const VenueChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
            child: Text('Venue Chat Screen')));
  }
}
